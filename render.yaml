services:
  - type: web
    name: rock-blast-fireball
    env: node
    buildCommand: echo "No build required"
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
